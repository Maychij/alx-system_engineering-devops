#!/usr/bin/env bash
# checking for command line arguments

if [ $# -lt 4]
then
echo "Usage: 0-transfer_file PATH_TO_FILE IP USERNAME PATH_TO_SSH_KEY"
exit 1
fi

# assigning parameters to variables

path_to_files=$1
ip=#$2
username=$3
path_to_ssh_key=$4

# dissabling strict host checking

